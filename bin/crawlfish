#!/bin/bash

if [[ $# -eq 0 ]]; then
  echo "Usage: crawlfish <hostname>"
  exit
fi

export CRAWLFISH_PORT=$(jot -r 1 1024-65535)
HOSTNAME=$1
shift

ssh -o SendEnv=CRAWLFISH_PORT -R $CRAWLFISH_PORT:localhost:22 $HOSTNAME "$@"
